{
  "name": "ccw-repeater-e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "docker-install": "yarn && yarn cypress install",
    "start": "wait-on http://app:3000 && yarn cypress run",
    "docker-run": "sh run-docker.sh",
    "docker-build": "sh build-docker.sh"
  },
  "description": "End to end tests for ccw repeater",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cypress": "^3.2.0",
    "wait-on": "^3.2.0"
  }
}
